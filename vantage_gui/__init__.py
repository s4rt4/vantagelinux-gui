"""Vantage GUI — a PyQt6 front-end mimicking Lenovo Vantage for Linux.

This package is front-end only. Hardware logic lives in `backend.py` (stubbed
with dummy data during the static-mockup phase) so the UI can be reviewed before
being wired to real sysfs / nmcli / pactl calls.
"""

__version__ = "0.1.0"
